# Bitácora — 2026-07-15

## Objetivo del día
Fase 2 del refactor del backend: simplificar controladores, eliminar código duplicado y
separar responsabilidades (reportes, auditoría) en clases dedicadas.

## Qué se hizo
- **Trait `CatalogoCrud`** (`app/Http/Controllers/Concerns/CatalogoCrud.php`): creación de
  un trait reutilizable que concentra la lógica común de los 6 controladores de catálogos
  simples (Gestión, Periodo, Carrera, Materia, Docente, Grupo). Cada controlador ahora
  solo declara qué modelo usa, qué reglas de validación tiene y cómo se llama la entidad;
  el trait provee `store()`, `actualizarModelo()` y `eliminarModelo()`. Se eliminó ~240
  líneas de código repetido entre los 6 controladores.
- **Corrección de `CarreraFactory`**: cambiado de `lexify('???')` a `bothify('??##')` para
  evitar colisiones con los datos semilla (el constraint UNIQUE en `sigla` fallaba en tests).
- **Corrección de reglas de validación**: `MateriaController` no validaba `carrera_id` ni
  `horas` (campos NOT NULL en la BD); `GrupoController` tenía reglas inventadas (`nombre`,
  `carrera_id`, `nivel`, `turno`, `activo`) que no coincidían con la BD real
  (`materia_id`, `codigo`, `estado`). Ambas corregidas.
- **Extracción de `crearEnLote()`** (`DesignacionMasivaController`): la lógica duplicada de
  crear designaciones en bloque (transacción, chequeo de choque, creación) aparecía
  idéntica en `asignarStore` y `copiarStore`. Ahora ambas llaman a un único método privado
  `crearEnLote()` que recibe un Closure para decidir qué filas incluir.
- **`DesignacionReportService`** (`app/Support/DesignacionReportService.php`): nueva clase
  que concentra las consultas pesadas de reportes que antes vivían mezcladas en los
  controladores: resumen por carrera, resumen por materia dentro de una carrera, y datos
  del dashboard (grupos sin designar, conteo por estado, docentes bajo límite de horas).
  Los controladores `DesignacionController` y `DashboardController` ahora delegan a este
  servicio.
- **`DesignacionObserver`** (`app/Observers/DesignacionObserver.php`): la lógica de
  registrar historial de cambios al editar una designación (comparar campos antes/después,
  crear registros en `designaciones_historial`) se movió del método `update()` del
  controlador a un Observer de Eloquent que se dispara automáticamente en el hook
  `updating`. Registrado en `AppServiceProvider`.
- Verificado: 42 tests pasando, Pint limpio en todos los archivos.

## Decisiones tomadas
- El trait `CatalogoCrud` no incluye `update()`/`destroy()` como métodos del trait con
  type hints de modelo, porque Laravel necesita el tipo concreto para el Route Model
  Binding. En su lugar, provee helpers protegidos (`actualizarModelo`, `eliminarModelo`)
  que cada controlador llama desde sus propios métodos `update`/`destroy` con el type hint
  correcto.
- `DesignacionObserver` usa `auth()->id()` para obtener el usuario porque el sistema tiene
  middleware de sesión siempre activo; no es necesario pasar el request manualmente.

## Bloqueos / dudas para el supervisor
Ninguno este día.

## Próximos pasos
- Considerar extraer `validarDatos()` de `DesignacionController` a un Form Request dedicado.
- Evaluar si `resumenCarga()` del mismo controlador debería moverse también al service.

## Tiempo invertido
~1.5 horas

---

## Sesión 2 (mismo día) — Estandarización de badges + tabla de asignación rápida

Trabajo en paralelo al refactor de arriba (otra herramienta de IA, coordinado sin tocar
los mismos archivos). Dos entregas distintas:

### Objetivo del día
Estandarizar el diseño de los badges de estado en toda la web, y — a partir de un
mockup revisado con el dueño — agregar una forma más rápida de asignar/reasignar
docentes desde `Carrera.jsx`.

### Qué se hizo
- **Badges de estado estandarizados**: nuevo componente `Components/Badge.jsx` (fondo
  blanco, borde e ícono del color del estado, en vez del relleno sólido + punto que
  usaba cada pantalla por su cuenta). Reemplaza el patrón viejo en los 4 lugares que
  tenían badges (`Grupos/Index.jsx`, `PorCarrera.jsx`, `Carrera.jsx`, `Lista.jsx`).
  De paso se corrigió que "propuesta" estaba en gris en dos pantallas cuando
  `design.md` ya definía "pendiente" como ámbar — ahora es consistente. `design.md`
  actualizado con la sección "Badges de estado" reescrita.
- **Plan de simplificación (`docs/PLAN_SIMPLIFICACION.md`)**: se armó un plan de 7 pasos
  para reducir pantallas/rutas de designaciones, con un mockup interactivo (HTML) del
  concepto central — una tabla única y editable por carrera, reemplazando varias
  pantallas. El dueño revisó el mockup y **solo aprobó una pieza de la idea**: la tabla
  editable con selector de "docente del pasado", pidiendo implementarla directo en la
  pantalla real sin tocar ni borrar ninguna otra. El documento del plan quedó marcado
  como referencia histórica (no un plan activo) — ver la nota al principio del archivo.
- **Tabla de asignación rápida en `Carrera.jsx`**: la pestaña "Asignación de docentes"
  (ahora la primera, por defecto) muestra una fila por grupo — asignado o no — con el
  docente editable ahí mismo (sin abrir otra pantalla), aviso de horas/choque de
  horario por fila, y un botón de historial que muestra los docentes que dictaron ese
  grupo en gestiones/periodos anteriores (más reciente primero) para poder proponerlos
  con un clic. Los cambios se guardan en bloque con un botón "Guardar cambios" que solo
  aparece si hay algo pendiente. Una designación `rechazada` se puede reasignar desde la
  misma fila (vuelve a quedar `propuesta`), sin pantalla ni botón especial.
- Backend: la acción `carrera()` de `DesignacionController` ahora arma también `roster`
  (una fila por grupo habilitado, con o sin designación) y `historialPorGrupo`; nueva
  acción `guardarRoster()` que hace upsert por fila en una sola petición (reasigna,
  crea, o borra si se deja sin asignar). Nueva ruta `designaciones.carrera.guardar`.
- Probado en el navegador contra datos reales (usuario `abdair@uatf.edu.bo`, carrera
  Administración de Empresas): asignar un grupo vacío, guardar, ver el progreso
  actualizarse en vivo, y abrir el historial de una fila con designaciones anteriores.
  Se encontró y corrigió ahí mismo un error de orden en el historial (el periodo 2 de
  una gestión aparecía después del periodo 1 de la misma gestión — faltaba un criterio
  de orden secundario).

### Decisiones tomadas
- Ninguna otra pantalla de designaciones (`PorCarrera`, `Lista`, `Create`, `Edit`,
  `AsignarPorCarrera`, `Copiar`) se tocó ni se eliminó — el pedido fue explícitamente
  acotado a esta tabla dentro de `Carrera.jsx`.
- El "historial de docentes" del selector rápido es por **grupo**, no por materia (una
  materia puede tener el grupo A y B con líneas de docentes distintas) — se interpretó
  así aunque el pedido decía "de esa materia", por ser la lectura más útil dado que el
  dominio ya trata groups como la unidad reutilizable entre periodos.
- El aviso de horas/choque de una fila se oculta mientras esa fila tiene un cambio sin
  guardar, para no mostrar una advertencia calculada sobre el docente anterior.

### Bloqueos / dudas para el supervisor
Ninguno este día.

### Próximos pasos
Ninguno planificado para esta pieza — queda a la espera de más pedidos puntuales. El
plan de 7 pasos original no se retoma sin confirmarlo de nuevo con el dueño.

### Tiempo invertido
~2 horas

---

## Sesión 3 (mismo día) — Optimización Frontend (Trabajador B: Tareas B1-B5)

### Objetivo del día
Implementar de manera rigurosa las tareas B1, B2, B3, B4 y B5 asignadas al Trabajador B del plan de optimización frontend (`docs/PLAN_FRONTEND.md`), unificando y simplificando los componentes comunes de formularios, tablas, acciones de fila, gráficos y estilos.

### Qué se hizo
- **Vite Path Alias** (`vite.config.js`): Configuración del alias `@` apuntando a `./resources/js` para permitir importaciones consistentes y limpias de componentes.
- **Componente `FormActions`** (`Components/FormActions.jsx`): Creación de un componente reutilizable para unificar las acciones de guardar y cancelar en los formularios con estilos premium consistentes.
- **Refactorización de Vistas de Creación y Edición (Tarea B1)**: Reemplazo de botones inline por `<FormActions>` y limpieza de importaciones de `Link` no utilizadas en las 14 vistas correspondientes:
  - `Carreras/Create.jsx` y `Carreras/Edit.jsx`
  - `Periodos/Create.jsx` y `Periodos/Edit.jsx`
  - `Gestiones/Create.jsx` y `Gestiones/Edit.jsx`
  - `Docentes/Create.jsx` y `Docentes/Edit.jsx`
  - `Materias/Create.jsx` y `Materias/Edit.jsx`
  - `Grupos/Create.jsx` y `Grupos/Edit.jsx`
  - `Designaciones/Create.jsx` y `Designaciones/Edit.jsx`
- **Componente `DataTable`** (`Components/DataTable.jsx`): Creación del envoltorio reutilizable para grillas de tablas.
- **Componente `FilaAcciones`** (`Components/FilaAcciones.jsx`): Creación del componente de acciones de fila (Editar y Eliminar) con soporte opcional para botones adicionales (`children`).
- **Refactorización de Tablas de Listado/Index (Tareas B2 y B3)**: Integración de `<DataTable>` y `<FilaAcciones>` en las 7 vistas de listas/index del sistema:
  - `Carreras/Index.jsx`
  - `Periodos/Index.jsx`
  - `Gestiones/Index.jsx`
  - `Docentes/Index.jsx` (además se corrigió el error de sintaxis por la doble declaración de `cambiarBusqueda` derivado de la migración de `useDebouncedSearch`)
  - `Materias/Index.jsx`
  - `Grupos/Index.jsx`
  - `Designaciones/Lista.jsx` (conservando el botón de historial en los slots de hijos de `FilaAcciones`)
- **Refactorización de Gráficos (Tarea B4)**: Creación de los componentes reutilizables `GraficoAnillo.jsx` y `BarraProgreso.jsx`, y actualización de `Designaciones/PorCarrera.jsx` para utilizarlos, reduciendo la duplicación local de gráficos.
- **Refactorización de Estilos de Login y Copiar (Tarea B5)**:
  - `Auth/Login.jsx`: Rediseño visual unificado con bordes redondeados `xl`, inputs y botones premium alineados con el resto del diseño del sistema.
  - `Designaciones/Copiar.jsx`: Reemplazo del listado en formato de tabla por una grilla interactiva de tarjetas (cards) con estados de hover refinados (`hover:border-blue-500/30 hover:bg-blue-50/10`) y checkboxes redondeados (`rounded-full`) funcionales al hacer clic sobre cualquier sector de la card.
- **Verificación**: Ejecución exitosa de `npm run build` y aprobación de los 60 tests del backend con `php artisan test`.

### Decisiones tomadas
- Se le dio soporte a `children` en `FilaAcciones` para poder insertar el botón de historial específico en `Designaciones/Lista.jsx` sin perder la abstracción y estandarización del componente.
- Se respetó estrictamente la lista de exclusión ("Qué NO tocar"), dejando intacta la vista `Designaciones/Carrera.jsx` en la tarea B4.

### Bloqueos / dudas para el supervisor
Ninguno este día.

### Próximos pasos
- Coordinar con el Trabajador A para validar la finalización completa del plan frontend.

### Tiempo invertido
~2.5 horas

---

## Sesión 4 (mismo día) — Optimización Frontend (Trabajador A + B5 restante)

### Objetivo del día
Completar las tareas del Trabajador A del plan de optimización frontend (hooks
reutilizables + FilterBar expandido) y cerrar la tarea B5 pendiente (unificación de
estilos en Login y Copiar).

### Qué se hizo
- **`useDebouncedSearch` hook** (`resources/js/Hooks/useDebouncedSearch.js`): hook
  reutilizable que encapsula el patrón de búsqueda con debounce de 350ms. Acepta
  configuración de `only` para recarga parcial de Inertia y `preserveScroll`. Reemplaza
  el código duplicado en `Docentes/Index.jsx` y `Designaciones/PorCarrera.jsx`.
- **`useResumenCarga` hook** (`resources/js/Hooks/useResumenCarga.js`): hook reutilizable
  que observa los campos del formulario de designación (`Id_docente`, `Id_materia`,
  `Id_grupo`, `Id_gestion`, `Id_periodo`) y dispara un `router.reload` parcial cada vez
  que cambian, para obtener el resumen de carga actualizado del backend. Reemplaza el
  `useEffect` duplicado en `Designaciones/Create.jsx` y `Designaciones/Edit.jsx`.
- **`FilterBar` expandido** (`resources/js/Components/FilterBar.jsx`): se agregó soporte
  opcional para un campo de búsqueda con texto (input con debounce) a la izquierda de los
  selects. Los props `busqueda` y `onBusquedaChange` son opcionales — si no se pasan, solo
  se renderizan los selects como antes.
- **Migración de 4 páginas a FilterBar**:
  - `Docentes/Index.jsx`: reemplazado el bloque de filtros inline (input + select + botón)
    por `<FilterBar>` con búsqueda + select de carrera.
  - `Materias/Index.jsx`: reemplazado select inline por `<FilterBar>` con select de carrera.
  - `Grupos/Index.jsx`: reemplazados selects de materia y estado por `<FilterBar>`.
  - `Designaciones/PorCarrera.jsx`: reemplazado el bloque de filtros inline (búsqueda +
    gestión + periodo + estado) por `<FilterBar>`.
- **B5 completada — Login.jsx**: eliminada la constante local `inputClass` y reemplazada
  con `import { inputClass } from '@/Components/Field'`. El estilo ahora es consistente
  con el resto del sistema (`rounded-lg border-gray-200` en vez de `rounded-md border-gray-300`).
- **B5 completada — Copiar.jsx**: eliminada la constante local `selectClass` y reemplazados
  los 5 elementos `<select>` nativos por el componente `<Select>` compartido, que incluye
  un ícono de chevron abajo y estilo consistente.
- **Verificación**: `npm run build` sin errores (416.84 kB JS gzip 116.07 kB), 60/60 tests
  de backend pasando.

### Decisiones tomadas
- `useDebouncedSearch` soporta `only` y `preserveScroll` como opciones porque tanto
  `Docentes/Index` como `PorCarrera` hacen recarga parcial (no recargan toda la página).
  Sin esto, el debounce perdía la funcionalidad de recarga parcial.
- `FilterBar` usa props separados (`busqueda`, `onBusquedaChange`) en vez de un objeto
  `buscar` como proponía el plan original, para ser más consistente con los otros props
  del componente.
- No se tocó `Carrera.jsx` (está en la lista de exclusión) — el sub-componente
  `AnilloProgreso` quedó ahí mientras que `Dona` y `BarraProgreso` de `PorCarrera.jsx`
  sí se extrajeron a componentes separados.

### Bloqueos / dudas para el supervisor
Ninguno este día.

### Próximos pasos
- Verificación final del plan completo: todos los archivos del plan (A1-A3, B1-B5)
  están completados y verificados.

### Tiempo invertido
~1 hora

