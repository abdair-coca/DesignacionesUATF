# Bitácora — 2026-07-26

## Objetivo del día
Corregir error `b.map is not a function` en el Dashboard al cargar después del inicio de sesión. Crear registro centralizado de tareas pendientes del proyecto.

## Qué se hizo
- **Bugfix — `DesignacionReportService.php`**: Se corrigió el formato de retorno de `evolucionDesignaciones()` que devolvía un objeto `{periodo, puntos}` en lugar del array simple `[{label, valor}]` que el componente React `Dashboard/Index.jsx` espera. Esto causaba el crash `b.map is not a function` al hacer `.map()` sobre un objeto.
- **Bugfix — `DesignacionReportService.php`**: Se agregaron los campos `nombre` y `sigla` al método `resumenPorCarrera()` para que el componente pueda mostrar el nombre de cada carrera en las barras de progreso.
- **Bugfix — `DesignacionReportService.php`**: Se agregó la clave `limiteHoras` faltante en `datosDashboard()` que el componente espera para mostrar el texto "Docentes bajo Xh".
- **Documentación — `docs/pendientes.md`**: Se creó un archivo vivo con todas las tareas pendientes del proyecto categorizadas (refactor frontend, rediseño dashboard, motivo rechazo, template, pendientes de supervisor). La regla es: al completar una tarea, se borra del archivo y se menciona en la bitácora.
- **Pruebas**: 72/72 tests pasando. `npm run build` exitoso.

## Decisiones tomadas
- **Formato de `evolucionDesignaciones()`**: Se cambió de `{periodo, puntos}` a `[{label, valor}]` para que coincida con lo que el frontend ya espera, en lugar de modificar el componente. Esto evita tocar código React ya compilado y reduce riesgo de regression.
- **Archivo de pendientes**: Se ubicó en `docs/pendientes.md` (no en `tasks/`) porque es un registro del proyecto, no parte del protocolo de orquestación de tareas.
- **Actualizar pendientes.md**: Solo se borra la tarea completada. No se reescribe el archivo completo a menos que haya cambios estructurales.

## Bloqueos / dudas para el supervisor
- Ninguno este día.

## Próximos pasos
- Continuar con las tareas listadas en `docs/pendientes.md` según prioridad del dueño del proyecto.

## Tiempo invertido
~1 hora
