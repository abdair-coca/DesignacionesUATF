# Bitácora — 2026-07-29

## Objetivo del día
Auditar, corregir y mejorar de manera integral el backend y las vistas del módulo del Director (`/designaciones/lista` y `carrera.blade.php`) y del Vicerrectorado (`/revisiones`), solucionando la selección explícita de propuestas por `revision_id`, decisiones no automáticas en la revisión, tarjetas de estadísticas y alertas docentes, e historial de reenvíos.

## Qué se hizo

- **Envío por Propuesta Específica (`RevisionController@solicitar` y `lista.blade.php`)**:
  - Cada fila en la lista del Director cuenta con un botón verde/cian **"Enviar"** que transmite el `revision_id` exacto para enviar a revisión únicamente la propuesta seleccionada.
- **Decisión Explícita sin Aprobaciones Falsas en Revisión (`RevisionController@completar` y `revisar.blade.php`)**:
  - Se eliminó el comportamiento automático de aprobar todo si se cerraba o cancelaba la pantalla.
  - Se añadieron dos botones explícitos: **"Aprobar Propuesta Completa"** y **"Devolver con Observaciones"** (con modal para ingresar observaciones al Director).
- **Tarjetas de Estadísticas y Alertas de Sobrecarga Docente**:
  - Se incorporaron métricas clave: Cobertura %, Total Horas, Docentes y Materias.
  - Se añadieron alertas visuales en caso de docentes con sobrecarga horaria (>32 hrs en UATF).
- **Historial de Reenvíos e Impugnaciones**:
  - Se añadió una sección de historial en la vista del Vicerrector para consultar versiones y observaciones previas de la misma carrera.
- **Modales UI Estilo Color Admin v2**:
  - Se reemplazaron todos los `alert()` y `confirm()` nativos por modales UI personalizados.
- **Cobertura de Pruebas Automatizadas**:
  - 83/83 pruebas pasando exitosamente en la suite global (`php artisan test`).
- **Fase 0, paso 1 completado**:
  - Se retiró la credencial de pruebas que estaba escrita en `phpunit.xml`.
  - Se agregó una plantilla `.env.testing.example` para configurar una base PostgreSQL exclusiva de testing.
- **Fase 0 completada**:
  - El login limita los intentos fallidos por correo e IP y reinicia el contador al autenticar correctamente.
  - Se actualizaron dependencias auditadas, incluido `guzzlehttp/guzzle`, y Composer ya no reporta avisos de seguridad.
  - Se aplicó Pint al código existente y se agregó CI con PostgreSQL efímero para pruebas, Pint y `composer audit --locked`.
- **Fase 1, paso 1 preparado**:
  - Se creó el respaldo PostgreSQL `pre_fase_1_20260729_182158.dump` y se verificó que `pg_restore` puede leerlo.
  - Se agregó una migración aditiva y reversible: `grupos.malla_curricular_id` nullable y `gestiones.es_actual` con exclusividad para una sola gestión actual.
  - La migración no asigna mallas históricas ni marca una gestión existente como actual; esas decisiones se dejan para los pasos posteriores del plan.
- **Fase 1, paso 2 preparado**:
  - El inventario encontró 151 materias, 167 filas de malla, cero materias sin carrera válida, cero mallas propias faltantes y cero duplicados.
  - Se agregó un backfill idempotente que crea solo la relación carrera-materia faltante y se detiene mostrando IDs si una materia no tiene carrera determinable.
  - Cada fila creada queda registrada para que una reversión elimine únicamente datos introducidos por este backfill.
- **Fase 1, paso 3 preparado**:
  - El inventario encontró 200 grupos, todos con malla propia resoluble; los 151 códigos `A` y 49 códigos `B` no generan colisiones al convertirse en `1` y `2`.
  - Se agregó una migración que asigna cada grupo a su malla propia, normaliza códigos alfabéticos o numéricos positivos y rechaza códigos inválidos o colisiones antes de modificar datos.
  - La migración registra los valores originales de cada grupo, por lo que una reversión restaura solo sus códigos y asociaciones anteriores.
- **Fase 1, paso 4 preparado**:
  - El inventario encontró 917 designaciones sin incompatibilidades materia-grupo, sin mallas faltantes y sin grupos activos duplicados.
  - Se agregó una migración que completa la malla de cada designación y garantiza por base de datos que grupo, malla y materia siempre correspondan entre sí.
  - Un índice parcial rechaza un segundo docente activo para el mismo grupo, gestión y período, mientras permite conservar registros rechazados como historial.

## Decisiones tomadas
- **Envíos por `revision_id`**: Evita ambigüedades cuando existen múltiples borradores para la misma gestión.
- **Decisión explícita**: La revisión solo se finaliza cuando el Vicerrector presiona un botón de decisión explícito.
- **Plan de elevación**: Se documentó modelo objetivo aprobado para normalización académica, roles, propuestas versionadas, revisión, importación, notificaciones y calidad.
- **Testing aislado**: Las pruebas quedan apuntando por defecto a `designaciones_uatf_testing`; usuario y contrasena se cargan desde `.env.testing` local para no volver a exponer secretos.
- **Migración gradual de grupos**: La nueva FK de malla es nullable para conservar todos los grupos existentes mientras se inventarían y migran sus asociaciones históricas.
- **Gestión actual explícita**: El nuevo indicador inicia en `false`; no se infiere desde el año del servidor ni desde el nombre de una gestión.
- **Backfill trazable**: Las filas nuevas de `malla_curricular` quedan en un registro técnico de migración; las asociaciones históricas que ya existían no se sobrescriben ni se eliminan al revertir.
- **Normalización sin suposiciones**: Un grupo ya asociado a una malla distinta de la que corresponde a su materia histórica bloquea la migración y reporta su ID.
- **Integridad de designaciones**: `Id_materia` se conserva como referencia directa, pero las nuevas claves compuestas impiden que contradiga al grupo o a la malla curricular.

## Bloqueos / dudas para el supervisor
- Para ejecutar la suite completa falta crear la base PostgreSQL `designaciones_uatf_testing` con un usuario que tenga permiso `CREATEDB`; el usuario local actual no puede crear bases.
- Falta validar una restauración completa del respaldo en una base PostgreSQL temporal, porque el usuario local no tiene permiso `CREATEDB`.

## Próximos pasos
- Crear una base PostgreSQL temporal, restaurar el respaldo y ejecutar la migración pendiente junto con la suite.
- Migrar designaciones y agregar restricciones de unicidad e integridad entre grupo, malla, gestión y período.

## Tiempo invertido
~4.5 horas
